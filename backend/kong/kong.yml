_format_version: "3.0"
_transform: true

services:
  - name: indicators-service
    url: http://indicators-service:8080
    routes:
      - name: indicadores-epidemiologicos
        paths: ["/api/indicadores/epidemiologicos"]
        methods: ["GET", "OPTIONS"]
        strip_path: false
      - name: indicadores-climaticos
        paths: ["/api/indicadores/climaticos"]
        methods: ["GET", "OPTIONS"]
        strip_path: false
      - name: indicadores-serie-mensal
        paths: ["/api/indicadores/serie-mensal"]
        methods: ["GET", "OPTIONS"]
        strip_path: false
      - name: vulnerabilidade
        paths: ["/api/vulnerabilidade"]
        methods: ["GET", "OPTIONS"]
        strip_path: false
      - name: alertas
        paths: ["/api/alertas"]
        methods: ["GET", "OPTIONS"]
        strip_path: false
      - name: municipios
        paths: ["/api/municipios"]
        methods: ["GET", "OPTIONS"]
        strip_path: false
      - name: gats
        paths: ["/api/gats"]
        methods: ["GET", "OPTIONS"]
        strip_path: false
      - name: pesquisadores
        paths: ["/api/pesquisadores"]
        methods: ["GET", "OPTIONS"]
        strip_path: false
      - name: dashboard-kpis
        paths: ["/api/dashboard/kpis"]
        methods: ["GET", "OPTIONS"]
        strip_path: false
      - name: conteudos
        paths: ["/api/conteudos"]
        methods: ["GET", "POST", "OPTIONS"]
        strip_path: false

  - name: import-service
    url: http://import-service:8081

    routes:
      - name: import-api
        paths:
          - /api/import
        methods:
          - GET
          - POST
          - PUT
          - DELETE
          - OPTIONS
        strip_path: false

  - name: search-service
    url: http://search-service:8080
    routes:
      - name: search
        paths: ["/api/search"]
        methods: ["GET", "OPTIONS"]
        strip_path: false

plugins:
  - name: cors
    config:
      origins: ["*"]
      methods: ["GET", "POST", "PUT", "DELETE", "OPTIONS"]
      headers: ["Accept", "Content-Type", "Authorization"]
      max_age: 3600

  - name: rate-limiting
    config:
      minute: 120
      policy: local
